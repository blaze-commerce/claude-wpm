# Blaze QA Environment Configuration
# Copy this file to .env and configure as needed

# Default environment to test (live, staging1, staging2)
QA_ENVIRONMENT=live

# CI mode (set by GitHub Actions)
CI=false

# Headless mode (set to false to see browser during tests)
HEADLESS=true

# Parallel workers (reduce if rate limiting occurs)
WORKERS=4
